import 'package:flutter/material.dart';

class ContactNotesTab extends StatelessWidget {
  const ContactNotesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Notes'),
    );
  }
}