import 'package:flutter/material.dart';

class ContactTimelineTab extends StatelessWidget {
  const ContactTimelineTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('History'),
    );
  }
}